import si from 'systeminformation'

export async function generate_command(filename:string,bitValue): Promise<string> {
    filename:
    outputfolder
    bitValue
    crfValue
    encoderValue
    qualityValue
    videoContainer
    AudioContainer
    isUseCrf
    isSaveAudio

//   const deviceList: Array<string> = []
//   const gpus = await si.graphics()
//   for (const i in gpus.controllers) {
//     deviceList.push(gpus.controllers[i].model)
//   }
//   return deviceList
}

// export async function generate_vpy(): Promise<string> {

//   }
